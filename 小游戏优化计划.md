# GB demo 内置小游戏优化计划（进度表）

> 更新日期：2026-09-23
> 关联文档：`小游戏美术素材清单.md`（素材组交付规格）

## 目标
优化 GB demo 三个内置小游戏（华容道 / 星空圆盘 / 星纹密码）的操作手感，并接入美术素材。

## 总原则
1. **逻辑层不动**：`*_logic.rpy`（状态、移动、胜利判定、存档）保留原样，只重写渲染 + 输入层（screen → custom displayable）。
2. **渲染层走 pygame**：Ren'Py 8 运行时自带 pygame（pygame_sdl2），在 custom displayable 的 `render()` / `event()` 里绘制和收事件，每帧流畅渲染。
3. **动工前预览**：每个游戏改造前先出改动方案，用户确认后实施。
4. **素材未到位前用占位图/色块开发**，素材组交付后只替换渲染部分。

## 推进流程（用户约定）
> 每个小游戏优化完 → 先自检能否启动 → 交用户验收 → 合格或按反馈修改，直到验收通过 → 再进入下一个小游戏。

## 进度总览

| 阶段 | 小游戏 | 状态 | 备注 |
|---|---|---|---|
| 1 | 星纹密码（最卡，先开刀） | ✅ 验收通过 | 真拖放 + 每帧渲染已落地；重置/旋转/取消均显式强制重绘 |
| 2 | 星空圆盘 | 🚧 进行中 | 合并渲染为单个 displayable，拖拽跟手 |
| 3 | 华容道 | ⬜ 未开始 | 方块滑动动画 + 鼠标拖拽 |
| 4 | 素材接入联调 | ⬜ 未开始 | 等素材组交付，替换色块渲染 |
| 5 | 整体验证 | ⬜ 未开始 | 手感 / 卡顿 / 胜利 / 存档兼容 |

## 各游戏方案要点

### 阶段 1：星纹密码
- **病根**：`ui.timer(0.016, repeat=True, action=renpy.restart_interaction)` 每 16ms 强制重启整个交互，整屏重绘必卡。
- **改造**：新建 `StarPatternDisplayable(renpy.Displayable)`：
  - `render()`：用 pygame Surface 每帧绘制网格、可用图形块、拖拽中的块（跟鼠标）、放置预览（绿/红）
  - `event()`：左键按下拿起、移动跟手、松手放置；R 旋转、右键/ C 取消
- **手感目标**：真拖放（替代"点选→移到→再点"）、60fps 无卡顿、胜利判定与原逻辑一致。

### 阶段 2：星空圆盘
- **病根**：50 个背景星点 + 圆环填充各自是独立 displayable，叠加渲染开销大。
- **改造**：游戏区域合并为单个 `StarDiskDisplayable`，一次绘制完成；拖拽旋转逻辑保留。
- **手感目标**：拖拽跟手、恒星发光贴图、接近目标高亮提示。

### 阶段 3：华容道
- **病根**：方块瞬移、只能点选 + 键盘。
- **改造**：方块 0.15s 滑动插值动画 + 鼠标拖拽，键盘保留。
- **手感目标**：拖动跟手、松手吸附落格、进度/存档兼容。

### 阶段 4：素材接入
- 素材组按 `小游戏美术素材清单.md` 交付后，替换渲染中的色块为 PNG。
- 素材路径：`game/images/microgames/<华容道|星空圆盘|星纹密码>/`

### 阶段 5：整体验证
- 三个游戏在 Ren'Py 中实跑：启动无报错、手感达标、胜利与存档正常。

## 验收标准（每阶段）
- [ ] 游戏可正常启动（无 traceback）
- [ ] 操作手感达标（用户实机验收）
- [ ] 胜利条件 / 关卡进度 / 存档兼容
- [ ] 无新增报错日志

## 参考资源
- Ren'Py 中文技术社区：https://www.renpy.cn/portal.php （遇问题优先查阅借鉴）
- Ren'Py 官方技术文档：https://www.renpy.org/doc/html/ （Custom Displayables：https://www.renpy.org/doc/html/cdd.html ）

## 方案可行性核验（2026-09-23）

**结论：custom displayable + pygame 方案可行，且是官方推荐的 2D 小游戏做法。**

1. **官方文档**（https://www.renpy.org/doc/html/cdd.html）：Creator-Defined Displayable 明确定位为"适合制作 Ren'Py 自带工具无法表达的 2D 小游戏"，可接收任意 pygame 事件，render() 返回 renpy.Render 并可 blit pygame Surface。
2. **Ren'Py 8.5.2 SDK 源码**（`renpy/pygame/__init__.py`）：提供 Surface / Rect / draw / image / transform / event 等完整 pygame API，`import pygame` 可用（pygame_sdl2 兼容层）。
3. **本项目现有代码已实锤**：`chinese_star_puzzle_logic.rpy` 中 `CircleOutline` 已在用 `import pygame` + `pygame.Surface` + `pygame.draw.circle` + `render.blit(surf)`，在 8.5.2 下运行正常。

**关键 API 要点（写代码时遵守）**：
- `event(ev, x, y, st)` 里的 x/y 是相对 displayable 左上角的坐标，优先使用（勿用 pygame 事件的绝对坐标）
- `render()` 必须返回 `renpy.Render`；pygame Surface 用 `render.blit(surf, (x, y))` 贴入
- 持续刷新用 `renpy.redraw(self, 0)` / `renpy.timeout()`，不要用 `ui.timer` 无条件刷屏
- 图片加载用 `renpy.load_surface()`（返回 pygame Surface）
- 键盘映射用 `renpy.map_event()`

## 变更记录
| 日期 | 内容 |
|---|---|
| 2026-09-23 | 计划建立；待用户确认阶段 1 改造预览 |
| 2026-09-23 | 记录参考资源；核验 custom displayable + pygame 方案可行性 |
| 2026-09-23 | 阶段1 星纹密码验收通过（每帧强制重绘方案解决重置不刷新）；进入阶段2 星空圆盘 |
